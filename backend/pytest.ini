[pytest]
testpaths = tests
markers =
    integration: requires running backend services or live websocket endpoints
    manual: diagnostic test intended for manual execution
    windows_com: requires Windows COM/Word automation
addopts = -m "not manual and not integration and not windows_com"
